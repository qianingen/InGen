## Week 1 Status

This repository currently contains the Week 1 Python development scaffold:

Python 3.11 package structure
pyproject.toml
pytest
black
flake8
strict mypy
pre-commit
GitHub Actions CI
first scaffold test

## Quickstart

python -m pip install -e ".[dev]"
python -m pytest
python -m pre_commit run --all-files